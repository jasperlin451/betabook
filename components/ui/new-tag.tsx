/** Shared release label for callouts, navigation, and feature headings. */
export function NewTag() {
  return (
    <span className="inline-flex shrink-0 items-center rounded-md bg-background px-2 py-0.5 text-xs font-semibold text-foreground">
      New
    </span>
  );
}
